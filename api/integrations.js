import {allowMethods,envState,github,hfCheck,send} from './_shared.js';
export default async function handler(req,res){if(!allowMethods(req,res,['GET']))return;const state=envState();const checks={huggingFace:{configured:state.huggingFace,ok:false},github:{configured:state.github,ok:false}};if(state.huggingFace)try{await hfCheck();checks.huggingFace.ok=true}catch(error){checks.huggingFace.error=error.message}if(state.github)try{const data=await github('/actions/workflows?per_page=1');checks.github.ok=true;checks.github.workflows=data.total_count}catch(error){checks.github.error=error.message}send(res,200,{...state,checks})}
