import {allowMethods,envState,github,send} from './_shared.js';
export default async function handler(req,res){if(!allowMethods(req,res,['GET','POST']))return;try{if(!envState().github)return send(res,503,{error:'Hãy cấu hình GitHub trên Vercel'});if(req.method==='GET'){const data=await github('/actions/runs?per_page=20');const runs=data.workflow_runs.map(run=>({id:run.id,name:run.name,status:run.status,conclusion:run.conclusion,createdAt:run.created_at,url:run.html_url}));return send(res,200,{total:data.total_count,runs})}const {projectId,workers=12}=req.body||{};if(!projectId)return send(res,400,{error:'Thiếu mã dự án'});const workflow=encodeURIComponent(process.env.GITHUB_WORKFLOW_FILE||'translate.yml');await github(`/actions/workflows/${workflow}/dispatches`,{method:'POST',body:JSON.stringify({ref:process.env.GITHUB_BRANCH||'main',inputs:{project_id:String(projectId),workers:String(Math.min(16,Math.max(1,Number(workers)||12)))}})});send(res,202,{ok:true,message:'Đã khởi chạy GitHub Actions'})}catch(error){send(res,500,{error:error.message})}}
